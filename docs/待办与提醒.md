# 待办与提醒（项目备忘）

> 维护人：网页端开发｜更新：2026-08-28（全面审查修复 + 流式改造完成后）

## ⏳ 待向交接人索取 / 补充
- [ ] ~~火山方舟账户欠费~~ **已解决**（2026-08-29 充值后实测文本/图像接口均恢复，海报端到端出图已验证）。
- [ ] 完整项目 git 仓库（.gitignore 已补齐，可放心初始化提交；注意 .env 已被忽略，切勿强行提交）。

## ✅ 已完成
- [x] 交接文档《网页端开发交接说明.pdf》已通读；全部项目文件已通读
- [x] **环境配置**：根目录 `.env`（scripts 用）+ `web/.env`（网页端用）均已配置；根 `.env.example` 已补
- [x] **热榜真实数据已通**（2026-08-29）：官方公共实例 api-hot.imsyy.top 域名已失效，fetch_hotlist.py 新增"直连模式"兜底（未配置 HOTLIST_API_BASE 时直连百度/头条/B站公开接口；微博/抖音需 cookie 抓不了）；web 端 /api/hotlist 不再依赖该环境变量，抓取失败才回退演示数据
- [x] **scripts 修复**：fetch_hotlist.py 补 `from __future__ import annotations`（Python 3.8 兼容）+ 缓存损坏防护；render_poster.py 卖点正则兼容纯文本/markdown 两种格式、字体路径配置化（POSTER_FONT 环境变量）；test_skill.py 轮次号改正则取最大值、extract_bg_prompt 截取顺序修正
- [x] **scripts/test_cases.md 已恢复**（从 4 份测试记录的输入行复原，17 条用例按 skill 分组）
- [x] **海报模板 T01/T02/T03 三套版式已落地**（render_poster.py；T01 上下双带 / T02 居中大标题 / T03 左侧文字栏；未知编号回落 T01）
- [x] **8 处 SKILL.md 失效链接修复**（references/ → examples/），Skill规范要点.md 同步更新
- [x] **web 端 API 加固**：role 白名单（防 system 注入）、消息条数/长度限制、SKILL.md 缺失明确报错
- [x] **SSE 流式输出已上线**（D-08）：文本逐字上屏；海报在文本流结束后渲染并有中间状态提示；mock 模式同步模拟流式
- [x] **海报管线潜伏 SyntaxError 修复**（内联 python -c 复合语句，此前海报渲染从未成功）；超时 120s→300s（实测带知识库首 token 需 116~131s）
- [x] **生成物治理**：web/.gitignore 忽略 public/generated/；bg 中间图渲染后自动删除；死代码与 lint 清零
- [x] **部署文档**：requirements.txt（Pillow + volcengine-sdk）；web/README.md 部署章节（Node 18+ / Python+字体 / 目录结构 / cwd 要求）
- [x] 图片识别能力就绪：tools/ocr.ps1（Windows 自带 OCR）

## 📌 开发要点备忘（精读结论）
- 后端 = 读 SKILL.md + knowledge 拼 system prompt → 调火山方舟（SSE 流式）→ 回显；海报管线调 render_poster.py（Python，child_process）
- 海报数据包解析须兼容两种格式（`【海报数据包】` / `# 【海报数据包】`），web 端 lib/poster.ts 与 Python 端 render_poster.py 双解析器均兼容
- 时令日历按月取数，必须用服务器真实当前日期
- 前端必须固定展示「AI 生成内容请人工核对后发布」；物料工坊开场 3 菜单引导（D-07）
- 乡土编剧是「先问 3 个问题再生成」引导式流程，靠全量历史消息维持会话状态

## 🚧 网页端开发进度（web/ 目录，Next.js 16 + TS + Tailwind）
- [x] 项目骨架 + 首页 4 功能入口 + 品牌栏 + 全局合规提示
- [x] 4 个功能聊天页 + /api/chat（SSE 流式）+ /api/hotlist（演示热榜兜底）+ 海报管线
- [x] 演示模式（无 Key 内置样例，流式行为与真实一致）
- [ ] **待办：账户充值后复测**——真实模式对话 + 海报端到端出图（Seedream 已开通，欠费是唯一阻塞）
- [x] **知识库精简提速**（2026-08-30，已生效验证）：乡土编剧/直播教练去掉 01 品牌介绍、物料工坊去掉运营经验库（10KB）。
      实测：物料工坊首 token 37.7s/总 42.9s（原 ~65s），乡土编剧首 token 7.9s/总 15.2s（原 ~116s），质量未退化
      （合规自查/AI起草提示/先问流程/产品卡人物均正常）。改回只需在 web/lib/skills.ts 加回一行。
- [ ] 路线A待办：火山方舟开通 doubao-seed-2-1-turbo-260628 可进一步提速（当前仅 doubao-seed-evolving 可用）
- [x] **热榜拒答问题已修复**（2026-08-30）：真实热榜含政治/灾难/八卦密集内容会触发模型安全拒答（"无法回答"）。
      修复：web/lib/server/ark.ts 新增 sanitizeHotlistText（程序层剔除政治/灾难/负面健康/八卦类条目，只对
      "这是今天的热榜"文本生效）+ buildSystemPrompt 追加「运行纪律」防拒答指令（忽略不可用条目，不许拒答）。
      实测：真实热榜 15s 正常输出 3 张卡片（时令日历 + 桂圆辟谣→蜂蜜 借势卡），不再拒答。
- [x] **输出格式漂移修复**（2026-08-30，待构建生效）：模型曾输出非标准卡片格式（无 ###、字段无冒号、带叙述开场）。
      修复①：buildSystemPrompt 追加「输出格式纪律」（强制按 SKILL 模板输出结构化格式、禁止叙述替代/解释输入）；
      修复②：web/lib/hotspot.ts 重写为容错解析器（卡片头容忍 ### 缺失与 ｜/: 分隔符；字段兼容"名称：值"同行、
      "名称：\n值"换行、字段名独占行无冒号三种写法；适配产品裸行兜底）——已用实测松散格式单测通过（4 用例全 PASS）。
      ⚠️ 需重新构建生效：cd web && npm run build && npm start（本地沙箱限制 build/dev 的 spawn，须在用户终端执行）
- [ ] 待办：实际部署（服务器 Node 18+ + Python + Pillow + 中文字体，见 web/README.md 部署章节）

> 本地启动：`cd web && npm run dev`（无 Key 自动演示模式）；npm 缓存已重定向到 `.npm-cache/`（沙箱限制，可删除）
